# Plan 05 — Release readiness (consumer surface)

**DONE 2026-06-14.** Steps A–E complete. Delivered: XML docs + a `PublicApiGenerator` public-API snapshot
guard; the consumer `README`; public-surface hardening (init/get-only/required modifiers, internalized
port-artifact helpers); the `GraphProvider.LadybugDb` + `AddGraphiti` primary names with `[Obsolete]`
`GraphProvider.Kuzu` / `AddGraphitiCore` aliases (GRPH0001/2); the InMemory-default constructor and
`AddEpisodeOptions`. The original LadybugDB package split (Step E) was later reversed by plan 06.

Durable record: surface/naming decisions in `decisions.md`, the matrix in `parity.md`, full work order in
git history. (Reduced to a stub 2026-06-28 per `doc-hygiene.md`.)
